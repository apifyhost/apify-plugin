# custom-header

Example Apify response-phase plugin.

Package it from the repository root:

```bash
go run ./cmd/apify-plugin pack --dir ./examples/plugins/custom-header --output ./custom-header-1.0.0.tar.gz
```
